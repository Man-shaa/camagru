version: '3.8'

services:
  web:
    build: .
    command: sh -c "npm run start:dev"
    ports:
      - "8080:8080"
    volumes:
      - ./src:/app/src
      - /app/node_modules
    environment:
      - NODE_ENV=development